# Project Working Rules

이 파일은 이 프로젝트에서 Codex가 개발 작업을 할 때 반드시 따라야 하는 운영 규칙이다.

## 로컬 서버 실행 금지

- 사용자가 명시적으로 요청하지 않는 한 로컬 개발 서버를 실행하지 않는다.
- 금지되는 대표 명령:
  - `npm run dev`
  - `npm run preview`
  - `vite --host`
  - `Start-Process ... npm ... dev`
  - 브라우저에서 `localhost`, `127.0.0.1` 앱을 열기 위한 서버 실행
- 화면 확인이 필요하면 먼저 대체 검증을 사용한다.
  - 정적 빌드: `npm run build`
  - 테스트: `npm test`
  - 파일/DOM 구조 검토
  - 필요한 경우 사용자에게 직접 실행 확인을 요청
- 예외: 사용자가 이번 턴에서 "로컬 서버 실행해줘", "브라우저로 직접 확인해줘"처럼 명확하게 허용한 경우에만 실행한다.

## 실패한 명령 반복 금지

- 같은 명령이 실패하면 원인을 분석하기 전에는 같은 명령을 다시 실행하지 않는다.
- 실패 후 다음 순서를 따른다.
  1. 에러 메시지의 핵심 원인, 파일, 패키지, 권한, 포트를 기록한다.
  2. 같은 실패가 환경 문제인지, 의존성 문제인지, 코드 문제인지 분류한다.
  3. 한 번에 하나의 가설만 세운다.
  4. 가설을 검증하는 더 작은 명령을 먼저 실행한다.
  5. 수정 또는 다른 명령은 원인이 확인된 뒤 실행한다.
- 샌드박스 권한 오류가 난 경우:
  - 같은 명령을 일반 모드로 반복하지 않는다.
  - 필요한 경우 즉시 `require_escalated`로 재실행하되, 왜 필요한지 짧게 설명한다.
- 의존성 충돌이 난 경우:
  - `--force`를 먼저 쓰지 않는다.
  - `package.json`, peer dependency 범위, lockfile 상태를 확인한다.
  - 공식 문서가 요구하는 버전과 현재 프로젝트 버전을 비교한 뒤 한 번만 조정한다.
- 포트 또는 dev server 문제:
  - 이 프로젝트에서는 기본적으로 해결을 위해 서버를 새로 띄우지 않는다.
  - 사용자가 허용한 경우에도 기존 프로세스/포트 상태를 먼저 확인하고, 같은 포트로 반복 실행하지 않는다.

## TDS 및 AppsInToss 작업 규칙

- AppsInToss 문서는 한국어 키워드로 먼저 검색한다.
- TDS WebView 프로젝트에서는 `@apps-in-toss/web-framework`와 `@toss/tds-mobile`, `@toss/tds-mobile-ait`의 peer dependency를 먼저 확인한다.
- TDS Mobile 공식 시작 문서 기준:
  - `@toss/tds-mobile`
  - `@toss/tds-mobile-ait`
  - `@emotion/react`
  - React 18 라인
- React 버전을 바꿀 때는 `npm ls react react-dom --depth=0`로 실제 설치 상태를 확인한다.
- 로컬 브라우저에서 Toss WebView native handler가 없어 발생하는 safe-area 관련 로그는 앱 런타임 오류와 구분한다.

## 검증 규칙

- 기본 검증 순서:
  1. `npm test`
  2. `npm run build`
  3. 필요한 경우 정적 파일 또는 소스 확인
- `typecheck` 스크립트가 없으면 임의로 있다고 가정하지 않는다.
- 검증 결과를 말할 때는 실제 실행한 명령과 결과만 보고한다.
- 로컬 서버 또는 브라우저 확인을 하지 않은 경우, 하지 않았다고 명확히 말한다.

## 반복 발생 가능 문제 방지

### 한글 텍스트 깨짐 방지

- Korean UI copy를 수정할 때는 파일을 읽은 결과가 깨져 보이면 바로 편집하지 않는다.
- 깨진 문자열을 기준으로 검색/테스트를 작성하지 않는다.
- 정상 원문이 필요한 경우 Stitch reference, 문서, 브라우저 표시 텍스트 등 신뢰 가능한 출처를 먼저 확인한다.
- 한글 문구를 대량 수정할 때는 변경 후 `Get-Content` 출력만 믿지 말고 테스트 또는 빌드 결과로 JSX 파싱이 정상인지 확인한다.

### 스크립트 존재 여부 착각 방지

- `npm run typecheck`, `npm run lint`처럼 있을 법한 스크립트를 실행하기 전에 `package.json`의 `scripts`를 확인한다.
- 스크립트가 없으면 새로 만들지 않는다. 사용자가 요청했거나 작업상 꼭 필요할 때만 추가한다.
- 최종 보고에는 "실행하지 못함"이 아니라 "프로젝트에 해당 스크립트가 없음"처럼 정확히 쓴다.

### Git 작업 트리 보호

- 작업 전후 `git status --short`로 내가 만든 변경과 기존 변경을 구분한다.
- 사용자가 만들었을 가능성이 있는 변경은 되돌리지 않는다.
- `dist`, `node_modules`, `package-lock.json`처럼 명령 실행으로 바뀌기 쉬운 파일은 수정 원인을 최종 보고에 분리해서 적는다.
- 정리 목적으로 `git reset`, `git checkout --`, 대량 삭제 명령을 사용하지 않는다.

### 의존성 설치 후 peer 누락 방지

- `npm install --legacy-peer-deps`를 사용한 뒤에는 테스트 관련 peer dependency가 빠졌는지 확인한다.
- `@testing-library/react`를 쓰는 프로젝트에서는 `@testing-library/dom` 존재 여부를 확인한다.
- `npm ls <주요 패키지> --depth=0`로 루트 설치 버전을 확인한다.
- audit 취약점 경고는 작업 범위와 분리해서 보고하고, 사용자 요청 없이 `npm audit fix --force`를 실행하지 않는다.

### 빌드 산출물 변경 관리

- `npm run build`는 `dist`를 변경할 수 있다는 점을 인지한다.
- 사용자가 산출물 커밋을 원하지 않은 경우, 최종 보고에서 `dist` 변경 가능성을 알려준다.
- 산출물 변경을 없애기 위해 임의로 삭제하거나 되돌리지 않는다.

### 브라우저 검증 과잉 방지

- 사용자가 로컬 서버 실행을 금지한 상태에서는 브라우저 검증을 시도하지 않는다.
- 시각 확인이 꼭 필요하면 먼저 사용자에게 허용을 묻는다.
- 브라우저 확인을 하지 못해도 테스트와 빌드로 검증 가능한 범위를 명확히 보고한다.

### 명령 실행 방식 정리

- 여러 파일 읽기는 병렬로 실행하되, 출력 구분용 `echo ====`, 명령 체이닝, 불필요한 파이프를 남발하지 않는다.
- 파일 생성/수정은 `apply_patch`를 우선 사용한다.
- 단순 파일 작성에 PowerShell heredoc, Python 스크립트, 임시 생성 스크립트를 쓰지 않는다.
- 실패한 명령의 출력이 길면 핵심 원인만 요약하고, 같은 방식으로 재시도하지 않는다.

## 작업 전 체크리스트

- 이번 작업에서 로컬 서버 실행이 필요한가? 사용자가 허용했는가?
- 실패한 명령을 그대로 반복하고 있지 않은가?
- 에러 원인을 읽고 분류했는가?
- 의존성 변경이면 공식 문서와 peer dependency를 확인했는가?
- 기존 사용자 변경을 되돌리지 않고 필요한 파일만 수정하고 있는가?
